module AdminService.Commands

open AdminService.Types

let ``create event`` (event : EventDetail) = async {
    
    return ()
} 

let ``update event`` (event : EventDetail) = async {
    
    return ()
} 

let ``create event ticket type`` (ticketType : TicketInfo) = async {
    return ()
}

let ``update event ticket type`` (ticketType : TicketInfo) = async {
    return ()
}